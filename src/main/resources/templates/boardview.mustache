<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>게시글 상세 페이지</title>
    <!-- 합쳐지고 최소화된 최신 CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <!-- 부가적인 테마 -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css">

</head>
<body>


<div class="box-body">
    <div class="form-group">
        <label>제목</label>
        <input type="text" name='title' class="form-control" th:value="${board.title}" readonly="readonly">
    </div>
    <div class="form-group">
        <label>내용</label>
        <textarea style="resize:none" type="text" rows="10" name="content" class="form-control" th:value="${board.content}" th:text="${board.content}" readonly="readonly"></textarea>
    </div>
</div>

<a th:href="@{/board/delete/(id=${board.id})}">글삭제</a>
<a th:href="@{${board.filepath}}">이미지다운</a>
<a th:href="@{/board/modify/{id}(id = ${board.id})}">수정</a>

<!--</div>-->
</body>
</html>